#include <Arduino.h>

// Compilation unit that pulls in the PROGMEM dashboard SPA so it links into the
// firmware. The actual array (DASHBOARD_HTML) lives in dashboard_html.inc.
#include "dashboard_html.inc"
